import type { Messages } from '.';

const messages: Messages = {
  home: {
    button: {
      create: 'Create Room',
      join: 'Join Room',
    },
    input: {
      placeholder: {
        playerName: 'Player Name',
        roomId: 'Room ID',
      }
    },
  },
};

export default messages;
