import type { Messages } from '.';

const messages: Messages = {
  room: {
    create: 'Create Room',
    join: 'Join Room',
    joinInput: 'Room ID',
    playerName: 'Player Name',
  },
};

export default messages;
